# 测试记录

更新时间：2026-05-02 00:10 CST

## 当前测试命令

后端完整测试：

```bash
cd backend
../.venv/bin/python manage.py test
```

后端配置检查：

```bash
cd backend
../.venv/bin/python manage.py check
```

前端构建：

```bash
cd frontend
npm run build
```

## 最近一次验证结果

- 后端完整测试：55 个测试通过。
- 后端模块测试：
  - `manage.py test masterdata`：6 个测试通过。
  - `manage.py test analytics`：13 个测试通过。
- 后端 check：通过。
- 前端构建：通过。

## 测试覆盖范围

### 预算审批与版本

- Draft 送审生成审批请求。
- 审批通过生成 V1。
- 审批退回回到 Draft。
- 已审批请求不能再次退回。
- Submitted 版本不能重复送审。
- Submitted/Approved 版本不能修改预算行和月度计划。
- Approved 版本可创建修订 Draft。
- 修订 Draft 可审批生成 V2。

### 版本 Diff

- 新增预算条目识别。
- 删除预算条目识别。
- 字段变化识别。
- 月度计划金额变化识别。
- Diff summary 统计校验。

### Dashboard

- `latest_approved` 默认口径。
- `current_draft` 口径。
- 部门聚焦筛选口径。
- 部门聚合。
- Category 聚合。
- 月度聚合。
- 二级部门用户只能看授权部门。
- 二级部门用户不能聚焦未授权部门。
- 支持创建个人看板配置。
- 部门共享看板必须落在有权限的部门范围内。
- 非一级预算角色不能创建全局看板配置。
- 同一范围默认看板配置互斥。
- 已保存看板配置可应用回预算概览。
- 非 owner 不能修改他人的共享看板配置。
- 匿名用户返回空业务数据。

### 主数据

- Category 可创建。
- Vendor 可更新。
- Region 可删除。
- Project 可创建并绑定 `Project Category` / `Product Line`。
- Project 可更新关联的 `Project Category` / `Product Line`。
- 历史采购推荐价接口返回过滤结果和推荐价。

### 权限

- 未登录访问当前用户 API 会被拒绝。
- 登录、当前用户、退出流程通过测试。
- 二级用户只能列表查看本部门预算表。
- 一级预算管理员可查看全量预算表。
- 二级用户只能查看本部门审批请求。
- 二级用户不能给其他部门预算表新增预算行。
- 二级用户不能提交其他部门版本。
- 无审批角色用户不能审批请求。

### 模板字段

- 模板字段按 order 返回。
- 重复字段 code 被拒绝。
- 一级预算管理员可创建字段。
- 一级预算管理员可更新字段名称和必填状态。
- 一级预算管理员可删除字段。
- 预算行动态必填字段校验。
- 金额和日期类型错误能定位到具体字段。
- PATCH 动态字段合并已有 JSON。

### Seed 数据

- `seed_demo` 连续运行保持幂等。
- 自动创建一级部门 `SS`。
- 自动创建 10 个二级部门：`Arch`、`PVE`、`PE`、`STE`、`PHE`、`FTE`、`cSSD_FW`、`eSSD_FW`、`Embedded_FW`、`PDT`。
- 自动生成 Draft、Approved、Submitted、修订 Draft 和待审批请求。

## 注意事项

- 从仓库根目录直接运行 `.venv/bin/python backend/manage.py test` 可能无法发现测试。
- 应在 `backend/` 目录运行 `../.venv/bin/python manage.py test`。
- `.venv/`、`node_modules/`、`frontend/dist/`、`backend/db.sqlite3` 不进入版本库。
